apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  creationTimestamp: null
  name: scylladbmanagerclusterregistrations.scylla.scylladb.com
spec:
  group: scylla.scylladb.com
  names:
    kind: ScyllaDBManagerClusterRegistration
    listKind: ScyllaDBManagerClusterRegistrationList
    plural: scylladbmanagerclusterregistrations
    singular: scylladbmanagerclusterregistration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Progressing')].status
      name: PROGRESSING
      type: string
    - jsonPath: .status.conditions[?(@.type=='Degraded')].status
      name: DEGRADED
      type: string
    - jsonPath: .status.conditions[?(@.type=='ManagerAPIConnectionVerified')].status
      name: MANAGER VERIFIED
      type: string
    - jsonPath: .status.conditions[?(@.type=='DatabaseConnectionVerified')].status
      name: DB VERIFIED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ScyllaDBManagerClusterRegistration.
            properties:
              authentication:
                description: |-
                  authentication configures database credentials. Values are read from Secrets only while
                  constructing a Manager request and are never persisted in this resource's status.
                properties:
                  alternator:
                    description: alternator configures access-key authentication for
                      Alternator.
                    properties:
                      accessKeyIDSecretKeyRef:
                        description: accessKeyIDSecretKeyRef selects the Alternator
                          access key ID from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      secretAccessKeySecretKeyRef:
                        description: secretAccessKeySecretKeyRef selects the Alternator
                          secret access key from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - accessKeyIDSecretKeyRef
                    - secretAccessKeySecretKeyRef
                    type: object
                  cql:
                    description: cql configures username and password authentication
                      for CQL.
                    properties:
                      passwordSecretKeyRef:
                        description: passwordSecretKeyRef selects the CQL password
                          from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      usernameSecretKeyRef:
                        description: usernameSecretKeyRef selects the CQL username
                          from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - passwordSecretKeyRef
                    - usernameSecretKeyRef
                    type: object
                required:
                - cql
                type: object
              managerAPI:
                description: managerAPI configures the verified mutual-TLS connection
                  from Operator to Manager.
                properties:
                  caConfigMapKeyRef:
                    description: |-
                      caConfigMapKeyRef selects the PEM CA bundle used to verify the Manager API.
                      Mutually exclusive with caSecretKeyRef.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the ConfigMap or its key must
                          be defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  caSecretKeyRef:
                    description: |-
                      caSecretKeyRef selects the PEM CA bundle used to verify the Manager API.
                      Mutually exclusive with caConfigMapKeyRef.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  clientCertificate:
                    description: clientCertificate configures the client identity
                      required by Manager mutual TLS.
                    properties:
                      certificateSecretKeyRef:
                        description: certificateSecretKeyRef selects a PEM client
                          certificate from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      privateKeySecretKeyRef:
                        description: privateKeySecretKeyRef selects the corresponding
                          PEM private key from a Secret in the registration namespace.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - certificateSecretKeyRef
                    - privateKeySecretKeyRef
                    type: object
                    x-kubernetes-validations:
                    - message: certificateSecretKeyRef.optional must be false
                      rule: '!has(self.certificateSecretKeyRef.optional) || !self.certificateSecretKeyRef.optional'
                    - message: privateKeySecretKeyRef.optional must be false
                      rule: '!has(self.privateKeySecretKeyRef.optional) || !self.privateKeySecretKeyRef.optional'
                  serverName:
                    description: serverName is the DNS name verified against the Manager
                      API serving certificate.
                    type: string
                  url:
                    description: url is the HTTPS base URL of the ScyllaDB Manager
                      API.
                    type: string
                required:
                - clientCertificate
                - serverName
                - url
                type: object
                x-kubernetes-validations:
                - message: exactly one of caConfigMapKeyRef or caSecretKeyRef is required
                  rule: has(self.caConfigMapKeyRef) != has(self.caSecretKeyRef)
                - message: caConfigMapKeyRef.optional must be false
                  rule: '!has(self.caConfigMapKeyRef) || !has(self.caConfigMapKeyRef.optional)
                    || !self.caConfigMapKeyRef.optional'
                - message: caSecretKeyRef.optional must be false
                  rule: '!has(self.caSecretKeyRef) || !has(self.caSecretKeyRef.optional)
                    || !self.caSecretKeyRef.optional'
              scyllaDBClusterRef:
                description: |-
                  scyllaDBClusterRef specifies the typed reference to the local ScyllaDB cluster.
                  Supported kinds are ScyllaCluster, ScyllaDBCluster, and ScyllaDBDatacenter in the
                  scylla.scylladb.com API group.
                properties:
                  kind:
                    description: kind specifies the type of the resource.
                    enum:
                    - ScyllaCluster
                    - ScyllaDBCluster
                    - ScyllaDBDatacenter
                    type: string
                  name:
                    description: name specifies the name of the resource in the same
                      namespace.
                    type: string
                required:
                - kind
                - name
                type: object
              tls:
                description: tls configures endpoint-specific CA trust and server-name
                  verification.
                properties:
                  agent:
                    description: agent configures strict ScyllaDB Manager Agent server
                      certificate verification.
                    properties:
                      caConfigMapKeyRef:
                        description: |-
                          caConfigMapKeyRef selects a PEM CA bundle from a ConfigMap in the registration namespace.
                          Mutually exclusive with caSecretKeyRef.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the ConfigMap or its key
                              must be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      caSecretKeyRef:
                        description: |-
                          caSecretKeyRef selects a PEM CA bundle from a Secret in the registration namespace.
                          Mutually exclusive with caConfigMapKeyRef.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      clientCertificate:
                        description: |-
                          clientCertificate configures the database client identity used for mutual TLS.
                          It is required for CQL and omitted for endpoint types that don't request client certificates.
                        properties:
                          certificateSecretKeyRef:
                            description: certificateSecretKeyRef selects a PEM client
                              certificate from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          privateKeySecretKeyRef:
                            description: privateKeySecretKeyRef selects the corresponding
                              PEM private key from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - certificateSecretKeyRef
                        - privateKeySecretKeyRef
                        type: object
                        x-kubernetes-validations:
                        - message: certificateSecretKeyRef.optional must be false
                          rule: '!has(self.certificateSecretKeyRef.optional) || !self.certificateSecretKeyRef.optional'
                        - message: privateKeySecretKeyRef.optional must be false
                          rule: '!has(self.privateKeySecretKeyRef.optional) || !self.privateKeySecretKeyRef.optional'
                      serverName:
                        description: serverName is the DNS name Manager must verify
                          against the serving certificate.
                        type: string
                    required:
                    - serverName
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of caConfigMapKeyRef or caSecretKeyRef
                        is required
                      rule: has(self.caConfigMapKeyRef) != has(self.caSecretKeyRef)
                    - message: caConfigMapKeyRef.optional must be false
                      rule: '!has(self.caConfigMapKeyRef) || !has(self.caConfigMapKeyRef.optional)
                        || !self.caConfigMapKeyRef.optional'
                    - message: caSecretKeyRef.optional must be false
                      rule: '!has(self.caSecretKeyRef) || !has(self.caSecretKeyRef.optional)
                        || !self.caSecretKeyRef.optional'
                  alternator:
                    description: alternator configures strict Alternator server certificate
                      verification.
                    properties:
                      caConfigMapKeyRef:
                        description: |-
                          caConfigMapKeyRef selects a PEM CA bundle from a ConfigMap in the registration namespace.
                          Mutually exclusive with caSecretKeyRef.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the ConfigMap or its key
                              must be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      caSecretKeyRef:
                        description: |-
                          caSecretKeyRef selects a PEM CA bundle from a Secret in the registration namespace.
                          Mutually exclusive with caConfigMapKeyRef.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      clientCertificate:
                        description: |-
                          clientCertificate configures the database client identity used for mutual TLS.
                          It is required for CQL and omitted for endpoint types that don't request client certificates.
                        properties:
                          certificateSecretKeyRef:
                            description: certificateSecretKeyRef selects a PEM client
                              certificate from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          privateKeySecretKeyRef:
                            description: privateKeySecretKeyRef selects the corresponding
                              PEM private key from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - certificateSecretKeyRef
                        - privateKeySecretKeyRef
                        type: object
                        x-kubernetes-validations:
                        - message: certificateSecretKeyRef.optional must be false
                          rule: '!has(self.certificateSecretKeyRef.optional) || !self.certificateSecretKeyRef.optional'
                        - message: privateKeySecretKeyRef.optional must be false
                          rule: '!has(self.privateKeySecretKeyRef.optional) || !self.privateKeySecretKeyRef.optional'
                      serverName:
                        description: serverName is the DNS name Manager must verify
                          against the serving certificate.
                        type: string
                    required:
                    - serverName
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of caConfigMapKeyRef or caSecretKeyRef
                        is required
                      rule: has(self.caConfigMapKeyRef) != has(self.caSecretKeyRef)
                    - message: caConfigMapKeyRef.optional must be false
                      rule: '!has(self.caConfigMapKeyRef) || !has(self.caConfigMapKeyRef.optional)
                        || !self.caConfigMapKeyRef.optional'
                    - message: caSecretKeyRef.optional must be false
                      rule: '!has(self.caSecretKeyRef) || !has(self.caSecretKeyRef.optional)
                        || !self.caSecretKeyRef.optional'
                  cql:
                    description: cql configures strict CQL server certificate verification.
                    properties:
                      caConfigMapKeyRef:
                        description: |-
                          caConfigMapKeyRef selects a PEM CA bundle from a ConfigMap in the registration namespace.
                          Mutually exclusive with caSecretKeyRef.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the ConfigMap or its key
                              must be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      caSecretKeyRef:
                        description: |-
                          caSecretKeyRef selects a PEM CA bundle from a Secret in the registration namespace.
                          Mutually exclusive with caConfigMapKeyRef.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      clientCertificate:
                        description: |-
                          clientCertificate configures the database client identity used for mutual TLS.
                          It is required for CQL and omitted for endpoint types that don't request client certificates.
                        properties:
                          certificateSecretKeyRef:
                            description: certificateSecretKeyRef selects a PEM client
                              certificate from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          privateKeySecretKeyRef:
                            description: privateKeySecretKeyRef selects the corresponding
                              PEM private key from a Secret in the registration namespace.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - certificateSecretKeyRef
                        - privateKeySecretKeyRef
                        type: object
                        x-kubernetes-validations:
                        - message: certificateSecretKeyRef.optional must be false
                          rule: '!has(self.certificateSecretKeyRef.optional) || !self.certificateSecretKeyRef.optional'
                        - message: privateKeySecretKeyRef.optional must be false
                          rule: '!has(self.privateKeySecretKeyRef.optional) || !self.privateKeySecretKeyRef.optional'
                      serverName:
                        description: serverName is the DNS name Manager must verify
                          against the serving certificate.
                        type: string
                    required:
                    - serverName
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of caConfigMapKeyRef or caSecretKeyRef
                        is required
                      rule: has(self.caConfigMapKeyRef) != has(self.caSecretKeyRef)
                    - message: caConfigMapKeyRef.optional must be false
                      rule: '!has(self.caConfigMapKeyRef) || !has(self.caConfigMapKeyRef.optional)
                        || !self.caConfigMapKeyRef.optional'
                    - message: caSecretKeyRef.optional must be false
                      rule: '!has(self.caSecretKeyRef) || !has(self.caSecretKeyRef.optional)
                        || !self.caSecretKeyRef.optional'
                required:
                - agent
                - cql
                type: object
                x-kubernetes-validations:
                - message: CQL clientCertificate is required
                  rule: has(self.cql) && has(self.cql.clientCertificate)
                - message: Alternator clientCertificate is not supported
                  rule: '!has(self.alternator) || !has(self.alternator.clientCertificate)'
                - message: Agent clientCertificate is not supported
                  rule: '!has(self.agent) || !has(self.agent.clientCertificate)'
            required:
            - authentication
            - managerAPI
            - scyllaDBClusterRef
            - tls
            type: object
          status:
            description: status reflects the observed state of ScyllaDBManagerClusterRegistration.
            properties:
              clusterID:
                description: clusterID reflects the internal identification number
                  of the cluster in ScyllaDB Manager state.
                type: string
              conditions:
                description: conditions hold conditions describing ScyllaDBManagerClusterRegistration
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionRevision:
                description: |-
                  connectionRevision is a non-secret digest of the desired connection metadata and referenced
                  object revisions. Secret data is deliberately excluded.
                type: string
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed for this ScyllaDBManagerClusterRegistration. It corresponds to the
                  ScyllaDBManagerClusterRegistration's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
